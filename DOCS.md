# 05.ru Tech Tower — документация

## Назначение проекта
Мини‑игра в стиле «stacker»: игрок строит башню из коробок/техники. Чем выше башня, тем лучше. На заданной высоте открывается промо‑экран с кодом. Игра запускается в браузере, рендер — Canvas 2D, UI — React.

## Быстрый запуск
1. `npm install`
2. `npm run dev`

## Структура проекта (ключевые файлы)
- `App.tsx` — корневой компонент, управление состоянием игры, вводом и счётом.
- `components/GameCanvas.tsx` — обёртка над `<canvas>`, создаёт `GameEngine`.
- `components/UIOverlay.tsx` — все экраны поверх игры и HUD.
- `utils/gameEngine.ts` — игровой движок и рендер.
- `constants.ts` — параметры игры, цвета, сообщения, эмодзи‑прогрессия.
- `types.ts` — типы и enum состояния.

## Состояния игры (FSM)
Состояния описаны в `types.ts`:
- `START` — стартовый экран и обучение.
- `PLAYING` — активная игра.
- `GAME_OVER` — экран завершения.
- `LEADERBOARD` — экран таблицы лидеров.

Переходы:
- `START → PLAYING` — кнопка «Начать» или клавиша `Space`.
- `PLAYING → GAME_OVER` — промах (блок полностью не пересёкся).
- `GAME_OVER → PLAYING` — кнопка «Попробовать снова» или `Space`.
- `START/GAME_OVER → LEADERBOARD` — кнопка «Таблица лидеров».
- `LEADERBOARD → (назад)` — кнопка «Назад» возвращает на предыдущий экран.

## Экраны и UI
Реализованы в `components/UIOverlay.tsx`:

1. **Стартовый экран (START)**
   - Разбит на 2 шага:
     - Экран 1: сюжет и мотивация (Рамадан), благотворительность, кнопка «Начать стройку».
     - Экран 2: правила, лестница наград и кнопка «Начать игру».

2. **HUD во время игры (PLAYING)**
   - Слева: текущая высота ("Этаж").
   - Справа: лучший результат ("Рекорд").
   - Вверху по центру: сколько этажей осталось до промокода и размер скидки.

3. **Экран завершения (GAME_OVER)**
   - Два варианта:
     - **Если очков > 0** — благотворительный попап с очками и суммой пожертвования.
     - Если достигнут уровень награды — в этом же экране показывается промокод за максимальный достигнутый уровень.
     - **Если очков = 0** — короткий экран «Башня упала» и кнопка повтора.
   - Бейдж «Новый рекорд», если обновлён best.
   - Кнопка «Сыграть ещё раз».
   - Кнопка «Таблица лидеров».

4. **Экран таблицы лидеров (LEADERBOARD)**
   - Список топ‑игроков с никнеймами и очками.
   - Подсветка первого места.
   - Блок «Твой рекорд».
   - Кнопка «Назад» возвращает на предыдущий экран.

## Страница для копирайтера и дизайнера
- Отдельный URL: `/pagefordesign`
- Содержит все тексты и состояния экранов, а также примеры UI‑блоков.
- Реализована в `components/DesignShowcase.tsx` и выбирается в `index.tsx`.

## Ввод и управление
- ПК: клавиша `Space`
  - `START`: начать игру
  - `PLAYING`: «тап» (установка блока)
  - `GAME_OVER`: рестарт
- Мобильные: `pointerdown` по экрану (в режиме `PLAYING`).
- Навигация к лидерам: кнопка «Таблица лидеров» доступна со стартового и финального экранов.

## Счёт и рекорды
- Счёт = количество успешно установленных блоков (уровень/этаж).
- Лучший результат хранится в `localStorage` по ключу `05ru_tech_tower_best`.
 
## Благотворительность
- Пожертвование начисляется при `GAME_OVER`, если итоговый счёт > 0.
- На экране завершения показываются очки и сумма пожертвования (1 очко = 1 ₽).
- Пояснение: в конце Рамадана проект перечисляет общую сумму на благотворительность.

## Таблица лидеров (Supabase)
- Данные хранятся в таблице `leaderboard_entries`.
- Никнейм пользователя генерируется один раз и сохраняется в `localStorage`.
- При окончании игры (`GAME_OVER`) сохраняется только максимальный результат пользователя (обновляется при улучшении).
- Экран лидеров запрашивает актуальные данные при открытии; если записей нет — показывается пустое состояние.

### Переменные окружения (Vite)
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

SQL‑схема и инструкции находятся в `supabase/leaderboard.sql` и `supabase/README.md`.

## Игровая логика (GameEngine)
Ключевые элементы находятся в `utils/gameEngine.ts`.

### Инициализация
- Создаётся Canvas и контекст.
- Генерируются звёзды для фона (`initStars`).
- Подписка на `resize`.
- Запуск бесконечного `requestAnimationFrame`‑цикла.

### Старт игры (`start()`)
- Обнуление блоков, мусора, текста.
- Сброс счёта, скорости, комбо‑серии.
- Создание базового блока внизу.
- Вызов `spawnNextBlock()`.

### Спавн блока (`spawnNextBlock()`)
- Новый блок появляется на высоте следующего уровня.
- Ширина равна предыдущему блоку.
- Начальная позиция — за экраном слева или справа (рандом).
- Эмодзи блока зависит от уровня (см. ниже).

### Обработка тапа (`handleInput()`)
1. **Проверка промаха**
   - Если `absDist >= width` → `GAME_OVER`.
2. **Проверка perfect‑попадания**
   - Если `absDist <= perfectTolerance`:
     - Блок идеально совмещается с предыдущим.
     - Увеличивается `perfectStreak`.
     - Каждые `comboThreshold` perfect‑ов:
       - Блок расширяется на `widthBonus`.
       - Появляется текст «БОНУС ШИРИНЫ!».
3. **Обрезка при неточности**
   - Излишек отрезается и падает как «мусор» (`debris`).
   - `perfectStreak` сбрасывается.
4. **Фиксация блока**
   - Блок добавляется в массив `blocks`.
   - Счёт увеличивается.
   - Скорость увеличивается.
5. **Промо‑логика**
   - Промокод показывается только после завершения игры.
   - Если достигнуто несколько уровней, показывается код за максимальный достигнутый уровень.

### Камера
- Камера плавно поднимается за башней.
- `cameraY` растёт до безопасной цели: `blocks * blockHeight - canvasHeight/2`.

### Рендер
- Фон: градиент сверху вниз.
- Звёзды: twinkle‑эффект через `sin`.
- Блоки: псевдо‑3D (верхняя и боковая грань).
- Мусор: падает с гравитацией и вращением.
- Floating‑тексты: всплывающие сообщения при perfect‑ах.

## Уровни и эмодзи
Функция `getEmojiForLevel(level)`:
- 1–5: 📱
- 6–10: 🎧
- 11–15: 💻
- 16–19: 📺
- 20+: 🕋

Базовый блок всегда 📦.

## Конфигурация игры (`constants.ts`)
- `baseWidth`: базовая ширина блока
- `blockHeight`: высота блока
- `initialSpeed`: начальная скорость
- `speedIncrement`: рост скорости за уровень
- `maxSpeed`: максимальная скорость
- `perfectTolerance`: допуск для «идеального» попадания
- `PROMO_REWARDS`: список уровней наград (score, code, discount)
- `comboThreshold`: серия perfect‑ов для бонуса
- `widthBonus`: бонусное расширение блока

## Важные детали реализации
- Canvas растягивается на весь экран, UI поверх него.
- Игра запускается сразу (анимация фона всегда активна), но логика ввода работает только в `PLAYING`.
- Промо‑экран ставит игру на паузу, но анимация фона продолжает идти.
## Награды и уровни
- 10 очков → промокод `RMLD` на скидку 1000 ₽
- 20 очков → промокод `HJKL` на скидку 1500 ₽
- 30 очков → промокод `KLSD` на скидку 2000 ₽
- Лестница наград показана на онбординге и в игре (следующая цель).
